Run a simulator from /simulator directory, then attack_route.py from /attack.

- /simulator/rentalSimulator.py is the simplified vulnerable API.
- /simulator/rentalSimulator_uuid.py is the pseudonymized version.
- /simulator/rentalSimulatorAggregate.py is the aggregate version.


/example_prediction contains a possible view of an attack on a pseudonymized API.
